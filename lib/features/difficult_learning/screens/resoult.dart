import 'package:flutter/material.dart';

class Resoult extends StatelessWidget {
  const Resoult({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
